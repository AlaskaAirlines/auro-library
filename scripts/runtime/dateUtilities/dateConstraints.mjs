export const DATE_UTIL_CONSTRAINTS = {
  maxDay: 31,
  maxMonth: 12,
  maxYear: 2400,
  minDay: 1,
  minMonth: 1,
  minYear: 1900,
};

/**
 * @description The maximum day value allowed by the various utilities.
 * @type {Number}
 */
export const {maxDay} = DATE_UTIL_CONSTRAINTS;

/**
 * @description The maximum month value allowed by the various utilities.
 * @type {Number}
 */
export const {maxMonth} = DATE_UTIL_CONSTRAINTS;

/**
 * @description The maximum year value allowed by the various utilities.
 * @type {Number}
 */
export const {maxYear} = DATE_UTIL_CONSTRAINTS;

/**
 * @description The minimum day value allowed by the various utilities.
 * @type {Number}
 */
export const {minDay} = DATE_UTIL_CONSTRAINTS;

/**
 * @description The minimum month value allowed by the various utilities.
 * @type {Number}
 */
export const {minMonth} = DATE_UTIL_CONSTRAINTS;

/**
 * @description The minimum year value allowed by the various utilities.
 * @type {Number}
 */
export const {minYear} = DATE_UTIL_CONSTRAINTS;
